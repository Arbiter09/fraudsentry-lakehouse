name: 'fraudsentry_dbt'
version: '1.0.0'
config-version: 2

profile: 'fraudsentry_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fraudsentry_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
